apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-cluster-kafka
  description: "quarkusdroneshop Kafka クラスター (AMQ Streams)"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: messaging
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-asite-orders-in
  description: "Kafka トピック: shop-asite.orders-in（注文チケット受信）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-bsite-orders-up
  description: "Kafka トピック: shop-bsite.orders-up（注文ステータス更新）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-asite-loyalty-updates
  description: "Kafka トピック: shop-asite.loyalty-updates（ロイヤリティ更新）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-asite-inventory-in
  description: "Kafka トピック: shop-asite.inventory-in（在庫受信）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-web-updates
  description: "Kafka トピック: web-updates（Web画面更新通知）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-loyalty-updates
  description: "Kafka トピック: loyalty-updates（ロイヤリティ更新通知）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-bsite-rewards
  description: "Kafka トピック: shop-bsite.rewards（リワードポイント受信）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka
---
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: shop-rewards
  description: "Kafka トピック: rewards（リワードポイント送信）"
  annotations:
    backstage.io/kubernetes-cluster: ${{ values.system_name }}
spec:
  type: kafka-topic
  lifecycle: production
  owner: ${{ values.owner }}
  system: ${{ values.system_name }}
  dependsOn:
    - resource:shop-cluster-kafka

apiVersion: v1
kind: Service
metadata:
  name: ${app_name}
  namespace: quarkusdroneshop-demo
  labels:
    app: ${app_name}
spec:
  selector:
    app: ${app_name}
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP